; Custom PHP settings for API container
upload_max_filesize = 50M
post_max_size = 100M
memory_limit = 512M
max_execution_time = 300
max_input_time = 300
; Increase input vars if needed for large JSON payloads
max_input_vars = 5000
; Disable exposing PHP version
expose_php = Off
